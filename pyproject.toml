[project]
name = "word-lists"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "httpx>=0.27",
    "wordfreq>=3.0",
    "pyspellchecker>=0.8",
]

[project.optional-dependencies]
dev = ["pytest>=9.0.3", "ruff>=0.4"]

[tool.ruff]
line-length = 100
